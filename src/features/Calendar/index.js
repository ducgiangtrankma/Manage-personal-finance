import React from 'react';
import {SafeAreaView, Text} from 'react-native';
import {styles} from './styles';
export default function Calendar(props) {
  return (
    <SafeAreaView style={styles.container}>
      <Text>Calendar scree</Text>
    </SafeAreaView>
  );
}
